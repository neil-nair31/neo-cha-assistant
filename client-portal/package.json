{
  "name": "@neo-cha/client-portal",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Neo Client Portal — container tracking, clearance timeline, documents & dispatch (Phase A)",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "dev:web": "npm run dev --prefix web",
    "build:web": "npm run build --prefix web",
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit",
    "smoke": "tsx src/smoke.ts"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.0.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
